// @ts-check

/** @type {import("prettier").Config} */
const config = {
	trailingComma: 'es5',
	useTabs: true,
	semi: true,
	singleQuote: true,
	printWidth: 120,
	arrowParens: 'avoid',
};

export default config;
