---
import '@/styles/fonts.css';
import '@/styles/global.css';

import BaseHead from '@components/base/BaseHead.astro';
import Footer from '@components/base/Footer.astro';
import Header from '@components/base/Header.astro';
import TransFlag from '@components/base/TransFlag.astro';

interface Props {
    title?: string;
    description?: string;
}
---

<!doctype html>
<html lang="en" class="mocha">
    <head>
        <BaseHead {...Astro.props} />
        <slot name="head" />
    </head>
    <body
        class="min-h-dvh w-screen bg-linear-to-br from-ctp-mantle via-ctp-crust to-ctp-mantle selection:bg-ctp-pink selection:text-ctp-crust"
    >
        <TransFlag />
        <div class="h-full macchiato pt-2">
            <Header />

            <div class="grid grid-cols-[1fr_min(calc(100%-1rem),72rem)_1fr] px-2 md:px-0 py-4">
                <slot name="left" />
                <main
                    class="bg-ctp-mantle p-3 rounded-lg border-2 border-dashed border-ctp-pink col-start-2"
                    transition:animate="fade"
                >
                    <slot />
                </main>
                <slot name="right" />
            </div>

            <Footer />
        </div>
    </body>
</html>
